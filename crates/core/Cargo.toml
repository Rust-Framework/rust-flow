[package]
name = "rust-agent-flow"
version = "0.1.0"
edition = "2021"
description = "Framework-agnostic workflow graph model and edge geometry for rust-agent-flow"
license = "MIT"

[dependencies]
slotmap = { workspace = true }
serde = { workspace = true }
serde_json = { version = "1" }
dagre = "0.1.1"
