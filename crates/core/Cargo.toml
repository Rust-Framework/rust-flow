[package]
name = "rust-agent-flow"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Framework-agnostic workflow graph model, geometry and layout (no GPUI)."
repository = "https://github.com/Rust-Framework/rust-flow"

[dependencies]
slotmap = { workspace = true }
serde = { workspace = true }
serde_json = { version = "1", features = ["preserve_order"] }
dagre = "0.1"
